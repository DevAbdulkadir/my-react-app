import './NavLink.css';

function NavLink() {
    return(
        <>
            <nav className="header__nav">
                <a href="#home">Home</a>
                <a href="#about">About</a>
                <a href="#contact">Contact</a>
            </nav>
        </>
    )
}

export default NavLink;